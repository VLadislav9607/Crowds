import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  label: {
    marginBottom: 14,
  },
  listContainer: {
    flexDirection: 'row',
    gap: 14,
  },
  checkboxContainer: {
    flex: 1,
  },
  errorMessage: {
    marginTop: 8,
  },
});
