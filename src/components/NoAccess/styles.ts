import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  noAccessImage: {
    width: 200,
    height: 200,
    alignSelf: 'center',
  },
  textCenter: {
    textAlign: 'center',
  },
});
