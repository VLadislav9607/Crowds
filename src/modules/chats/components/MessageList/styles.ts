import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  sectionHeader: {
    alignItems: 'center',
    paddingVertical: 16,
  },
  contentContainer: {
    flexGrow: 1,
    paddingHorizontal: 20,
    paddingBottom: 20,
  },
});
