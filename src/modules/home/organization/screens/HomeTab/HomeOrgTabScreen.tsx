import { ScreenWrapper } from '@components';
import { ReadyToAddEvent } from '../../ui';

export const HomeOrganizationTabScreen = () => {
  return (
    <ScreenWrapper headerVariant="withLogoAndImageBg">
      <ReadyToAddEvent />
    </ScreenWrapper>
  );
};
