import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 22,
  },
  title: {
    textAlign: 'center',
    marginTop: 24,
    marginBottom: 32,
  },
  resetPasswordButton: {
    marginTop: 62,
  },
});
