import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerContainer: {
    position: 'absolute',
    bottom: -120,
    zIndex: 100,
    width: '100%',
    paddingHorizontal: 20,
  },
});