import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  labelContainer: {
    flexDirection: 'row',
    gap: 4,
  },
  checkboxListContainer: {
    marginTop: 14,
  },
  inputContainer: {
    marginTop: 6,
  },
});
