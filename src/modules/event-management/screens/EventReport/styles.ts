import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 32,
    paddingHorizontal: 20,
    flexGrow: 1,
  },
  sectionTitle: {
    marginBottom: 20,
  },
});
