import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  list: {
    paddingTop: 16,
    paddingBottom: 80,
  },
  floatingButtonContainer: {
    position: 'absolute',
    bottom: 16,
    left: 20,
    right: 20,
  },
  floatingButton: {
    width: '100%',
  },
});
