import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  noPaddingBottom: {
    paddingBottom: 0,
  },
  floatingButtonContainer: {
    position: 'absolute',
    bottom: 32,
    left: 20,
    right: 20,
  },
  floatingButton: {
    width: '100%',
  },
});
