import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingHorizontal: 16,
    paddingTop: 16,
  },
  tabContainer: {
    paddingHorizontal: 16,
    paddingTop: 12,
  },
  skeletonContainer: {
    gap: 12,
    paddingTop: 8,
  },
  screenContainer: {
    paddingBottom: 0,
  },
});
